add a readme telling that external program is needed to create the exe called launch4j.
in any case include the already built program in the source.
add icon.

if pressing logout button game window should close (keep an array of getinstances()), or disable also logout button.

Make Top.java singleton?
Can we give up get/set currentWindow approach? if not we will have to update it everytime to be accurate...

onlogout close windows and update server also currentgame to null and broadcast waitingroom if needed.